---
name: user-flow-design
description: Convert PRODUCT_SPEC into complete navigation and state flows, including alternate and failure paths.
---

# User Flow Design

Input: `README.md`, `docs/PRODUCT_SPEC.md`.
Output: `docs/USER_FLOWS.md`.

Use `templates/USER_FLOWS.template.md`.

Ensure every MVP feature is reachable through at least one flow. Include first run, core loop, account lifecycle, destructive actions, permissions, errors, network loss, payments or moderation where applicable.

Do not invent screens without a product purpose.
